1、主键长度 varchar(80),  主键名：表名缩写_id

2、审核状态长度 varchar(2) 

3、活动金额相关字段  decimal(12,4)

4、数字相关字段 用 varchar

5、地点长度 varchar(100)

6、描述信息  varchar(1000)

7、所有表都要有 创建时间、创建人、修改时间、修改人,字段名不要有‘外键’字眼

8、时间字段都用  datetime

9、所有的关于文件、图片的内容建立公共的表，表名：‘文件地址表’

10、状态码的注释格式  例子：是否可用(01-可使用，02-不可用)，默认01